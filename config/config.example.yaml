# 班级标识：《大学物理实验》这类课程的作业名形如「国机2503-Thu1800-7 Thermal Conductivity」，
# 第一个 - 之前那段就是班级。填上自己的班级后，其他班的实验时段不再显示、也不提醒。
# 设置页可以直接从你的实际数据里挑，不必手写。
class_filter:
  enabled: true
  my_class: ""                  # 例：国机2503。留空 = 不过滤；只填前几位按前缀匹配
  courses:                     # 只对这些课程名生效
    - 大学物理实验
  separator: "-"
